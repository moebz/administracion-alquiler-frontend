# Cargado por Vite automáticamente al buildear en modo "production" (default
# de "vite build"/"refine build"), sin pisar frontend/.env (desarrollo). No
# es secreto — es la URL pública del backend, se commitea sin problema.
VITE_API_URL=https://inmovaapi.ebamworks.com/api
